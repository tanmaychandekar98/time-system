extends layout

block content
	#home
		h1= title
		p Welcome to #{title}
		.container
			hr
			if !user
				a.button.btn1(href='/signup/admin') Get Started as an admin
				span -----
				a.button.btn1(href='/signup/emp') Get Started as an employee*
				br
				br
				p *company key needed to register as an employee
			if user
				h3 Welcome #{user.name}
				if user.admin=="admin"
					a.btn1(href='/admin') #{user.company}
				else
					a.btn1(href='/users/'+user._id) #{user.name}
					
